[package]
name = "magic-packet"
version = "0.1.0"
authors = ["Armin Häberling <armin.aha@gmail.com>"]

build = "build.rs"

[build-dependencies]
clap = "2.9.2"
regex = "0.1.71"

[dependencies]
clap = "2.9.2"
regex = "0.1.71"
