[package]
name = "magic-packet"
version = "0.1.0"
authors = ["Armin Häberling <armin.aha@gmail.com>"]
license = "GPL-3.0"
readme = "README.md"
repository = "https://github.com/arminha/magic-packet"
homepage = "https://github.com/arminha/magic-packet"
description = "A command line tool for sending Wake-on-LAN Magic packets"

build = "build.rs"

[profile.release]
lto = true

[build-dependencies]
clap = "2.29"
regex = "0.2"

[dependencies]
clap = "2.29"
regex = "0.2"
